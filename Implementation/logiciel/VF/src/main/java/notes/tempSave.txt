try {

            System.out.print("Vos images actuelles sont: " + produitDetail.getImage() + "\n");

            String image;
            String imageURL = InputRestreint.getValidUrl("Veuillez entrer un url(ex: http://www.example.com):  ");

            String ligne = produitDetail.getImage();
            if (ligne.equals("non")){
                image = imageURL;
            }else{
                image = ligne + "; " + imageURL;
            }

            // ajuster l instance
            ProfilProduit.setImage(image);

            // affect le data du produit
            String path = DatabasePath.getProduitInfoPath() + produitDetail.getTitre() + "/main.csv";
            String temp = CSVHandler.readLineByIndex(path,1);
            String temp1 = CSVHandler.updateCSVColumn(temp,6,image);
            CSVHandler.coverCSV(path,temp1);

            // notification automatique
            String pathAuteurNotification = DatabasePath.getRevendeurComptePath() + revendeur + "/notification.csv";
            notificationSystemAuto(pathAuteurNotification, produitDetail.getTitre());

        } catch (Exception e) {
            System.out.println("Erreur: " + e.getMessage());
        }
    }