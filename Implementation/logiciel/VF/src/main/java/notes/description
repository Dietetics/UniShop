class done:

Database (but: refresh les datas vers un fichier principale)
GenerateurAuto (but: uniqueUUID)

CreateFileInDirectory (but: new file new directory) //auxiliaire pour test

ProfilProduit









test done:

offrir un produit a vendre
